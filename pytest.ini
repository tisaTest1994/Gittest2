[pytest]
markers=
    multiprocess: 可以多进程跑
    singleProcess: 一个个跑
    pro: 可以在生产环境上跑的用例


filterwarnings =
    error
    ignore::UserWarning

addopts = -v -s --tb=short
log_format = %(asctime)s %(levelname)s %(message)s
log_date_format = %Y-%m-%d %H:%M:%S
log_cli = false
log_level = INFO